import {BatteryCharging,Compass,Gauge,MapPin,Radio,ScanLine,Timer} from 'lucide-react'; import type {Telemetry as T} from '../types';
const items=(t:T)=>[{label:'Latitude',value:t.position.latitude.toFixed(6),unit:'°',icon:MapPin},{label:'Longitude',value:t.position.longitude.toFixed(6),unit:'°',icon:MapPin},{label:'Heading',value:t.heading_deg.toFixed(0),unit:'°',icon:Compass},{label:'Kecepatan',value:t.speed_mps.toFixed(1),unit:'m/s',icon:Gauge},{label:'Jarak Depan',value:t.front_distance_cm.toFixed(0),unit:'cm',icon:ScanLine},{label:'Baterai',value:t.battery_percent.toFixed(0),unit:'%',icon:BatteryCharging},{label:'Packet Loss',value:t.packet_loss_percent.toFixed(1),unit:'%',icon:Radio}];
export function Telemetry({data}:{data:T}){return <section className="telemetry"><div className="panel-title"><span>Telemetry</span><span className={data.connected?'signal on':'signal'}>{data.connected?'DATA LIVE':'OFFLINE'}</span></div><div className="metric-grid">{items(data).map(({label,value,unit,icon:Icon})=><div className="metric" key={label}><Icon size={17}/><small>{label}</small><strong>{value}<em>{unit}</em></strong></div>)}</div><div className="updated"><Timer size={14}/> Pembaruan terakhir {new Date(data.heartbeat_at).toLocaleTimeString('id-ID')}</div></section>}
